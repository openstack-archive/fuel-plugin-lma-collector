[<%= @title %>_decoder]
type = "SandboxDecoder"
filename = "<%= @filename %>"

<% if @config.size() > 0 %>
[<%= @title %>_decoder.config]
<%  @config.each do |k,v| %>
    <%= k %> = '<%= v %>'
<%  end %>
<% end %>
