[<%= @title %>_decoder]
type = "SandboxDecoder"
filename = "<%= @filename %>"

<% if @config.size() > 0 %>
[<%= @title %>_decoder.config]
<%  @config.each do |k,v| %>
    <%= k %> = <%= v.is_a?(String) ? "'#{v}'" : v %>
<%  end %>
<% end %>
